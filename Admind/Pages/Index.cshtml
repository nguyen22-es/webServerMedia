@page
@using Admin.Pages
@inject SignInManager<VODUser> SignInManager
@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<br />
@if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
{
    <div class="row">
        <div class="col-md-8 offset-md-2" style="padding-left:0; padding-right:0;">
            <alert alert-type="danger">@Model.Alert</alert>
        </div>
    </div>

    <div class="row">
        <div class="offset-sm-2 col-sm-8 offset-md-4 col-md-6">
            <partial name="_CardPartial" for="Cards.TopicTypes" />
            <partial name="_CardPartial" for="Cards.Users" />
        </div>
        <div class="offset-sm-2 col-sm-8 offset-md-4 col-md-6">
            <partial name="_CardPartial" for="Cards.Topics" />
            <partial name="_CardPartial" for="Cards.Modules" />
        </div>
        <div class="offset-sm-2 col-sm-8 offset-md-4 col-md-6">
            <partial name="_CardPartial" for="Cards.Medias" />
            <partial name="_CardPartial" for="Cards.Downloads" />
        </div>
    </div>
}

